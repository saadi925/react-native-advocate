import { Text, View } from 'native-base';
import React from 'react';
import { FriendRequest } from '../../../types/Cards';

interface Props {
  item : FriendRequest
}

const RequestCard: React.FC<Props> = ({item}) => {
  return (
  <View>
  </View>
  );
};

export default RequestCard;