import { View } from 'native-base'
import React from 'react'
import { LawyerCaseItem } from '../../../types/Cards'

export default function LawyerCaseCard({item} :{item : LawyerCaseItem}) {
  return (
    <View></View>
  )
}
