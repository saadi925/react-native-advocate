import { View } from 'native-base'
import React from 'react'
import { ClientCaseItem } from '../../../types/Cards'
type CaseCardProps = {
    item : ClientCaseItem
}

export default function ClientCaseCard({item} : CaseCardProps) {
  return (
  <View>

  </View>
  )
}
